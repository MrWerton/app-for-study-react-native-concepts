import { Text } from 'react-native';

import { Container } from './styles';

const Hooks: React.FC = () => {
  return (
    <Container>
      <Text>Hooks</Text>
    </Container>
  );
};

export default Hooks;
